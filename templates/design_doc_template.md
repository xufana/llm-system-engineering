# Design doc: [название продукта]

Основан на шаблоне команды [Reliable ML](https://github.com/IrinaGoloshchapova/ml_system_design_doc_ru/blob/main/ML_System_Design_Doc_Template.md) и на [design doc курса «Современные методы NLP»](https://github.com/xufana/modern_nlp_llm/blob/main/hw/hw_01.md); порядок разделов перестроен под методику этого курса: контракт → бюджеты → eval → baseline → усложнения.

| | |
|---|---|
| Авторы | [имена, группа] |
| Версия | 0.1 |
| Статус | черновик · на ревью · принят |
| Обновлён | [дата] |
| ADR | [ссылка на папку `adr/`] |

## Как пользоваться шаблоном

- Документ живой: заполняется по ходу курса и правится, когда меняется решение. Не переписывайте историю — добавляйте запись в «Историю изменений» внизу.
- Порядок разделов — это порядок работы. Не заполняйте раздел 6, пока пустой раздел 5.
- Всё в квадратных скобках заменить своим текстом. Раздел без единого числа считается незаполненным.
- Если пункт не применим — так и напишите: «не применимо, потому что …». Пустая строка и «не применимо» — разные вещи.
- Курсивом в разделах — вопросы, которые будут заданы на защите. Если на вопрос нет ответа в тексте, его зададут вслух.
- Каждое архитектурное решение, у которого были альтернативы, оформляется отдельным ADR (см. `adr_template.md`) и упоминается здесь ссылкой.

**Когда что заполнять**

| Раздел | Когда | Контрольная точка |
|---|---|---|
| 1 (контекст), 2 (контракт и сценарии), 3.1–3.4 (бюджеты), черновик 5.1 (20 запросов) | после занятия 1 | КТ 1 |
| 3.2 задержка и 3.3 стоимость — с расчётами по железу | после занятия 2 | КТ 1 |
| 4 (архитектура, данные, промпты), 6.1 (baseline) | после выбора стека | КТ 2 |
| 5 целиком (eval-план), 3.5 (SLI/SLO) | после занятия про eval, примерно неделя 6 | КТ 2 |
| 6.2 (усложнения), 7, 8, 9, 10, 11 | по мере проекта | защита |

---

## 1. Контекст проекта

### 1.1 Бизнес-задача

- **Проблема.** Что сейчас болит и у кого: [ускорение ответов поддержки, автоматизация FAQ, разбор обращений, …].
- **Почему без LLM-системы не справиться.** Чем не подходит поиск, правила, готовый продукт: [ ].
- **Почему именно это решение имеет смысл.** [ ]
- **Критерии успеха с точки зрения бизнеса.** Не метрики модели, а то, что изменится: [время ответа оператора −30 %, доля закрытых без человека обращений ≥ 40 %, …]. Отвечать на это в рамках курса не обязательно, но нужно представлять, чем будет измерен успех.

Если готовое решение уже есть — говорим об улучшении текущего опыта. Не пишите «сократим штат», даже если это цель: думайте об этике формулировок.

### 1.2 Пользователи и сценарии использования

- **Кто.** Не «сотрудники», а «специалисты поддержки первой линии, 40 человек»: [роль, сколько их, внутренние / клиенты / партнёры].
- **Через что.** [чат в вебе / виджет / мессенджер / голос / кнопка в интерфейсе / API для другой системы].
- **Нагрузка.** [сессий в день, запросов в месяц, пиковые часы и пиковый RPS]. Эти числа пойдут в бюджет стоимости и задержки.
- **Цена ошибки.** Что случится, если бот ответит неверно: [пользователь переспросит / оператор потратит время / потеря денег / репутационный или юридический риск]. От этого зависит, насколько строгий контракт нужен.
- **Что для пользователя «хорошо».** Чем он сам измерит пользу: [время, число ошибок, деньги, нервы].

*Где и с кем мы общаемся — определяет и вход, и выход: язык, тон, длину, что можно показывать.*

### 1.3 Границы, ограничения и допущения

**Границы системы.** Что внутри нашей ответственности, а что снаружи. За то, что снаружи, гарантий не даём.

| Внутри (наш код, наши данные) | Снаружи (провайдер, внешний API, чужой сервис) |
|---|---|
| [ ] | [ ] |

**Что явно не делаем.** 3–5 пунктов: [не интегрируемся с X, не обрабатываем Y, не отвечаем на вопросы вне домена].

**Допущения**, которые ещё не проверены: о данных [ ], об инфраструктуре [ ], о безопасности и нормативах [ ], о поведении пользователей [ ].

*Отвечаем ли на вопросы не по предметной области? Какие репутационные риски, если ответим?*

---

## 2. Контракт сервиса

Контракт — это то, что мы будем измерять. Каждый пункт превращается в метрику или тест в разделе 5. Если пункт нельзя измерить, это не контракт, а пожелание.

### 2.1 Контракт

| Поле | Что обещаем | Как проверим (ссылка на метрику из 5.2) |
|---|---|---|
| **Вход** | какие запросы принимаем: язык, длина, домен, форматы (текст / файлы / картинки); что делаем с остальными | [ ] |
| **Выход** | формат: свободный текст / JSON по схеме / ссылки на источники; гарантии: только из документов? может отказаться? | [ ] |
| **Поведение при неуверенности** | «не знаю» / уточняющий вопрос / передать оператору — и по какому признаку выбираем | [ ] |
| **Чего не обещаем** | точность вычислений, юридическую силу, отсутствие ошибок, … | [дисклеймер в интерфейсе / границы здесь] |
| **Нефункциональные** | задержка, доступность, приватность данных, где хранятся логи | [ ] |

**Контракт одной фразой** (пример из курса: «На вопрос на русском возвращает ответ, опирающийся на ≥1 фрагмент документации, со ссылкой на источник; если фрагментов нет — говорит об этом явно; первый токен ≤ 1,5 с, полный ответ ≤ 10 с; вне базы не отвечает»):

> [ваша фраза]

### 2.2 Сценарии взаимодействия

**Основные сценарии.** Для каждого: что говорит пользователь, что делает система, чем заканчивается.

| Сценарий | Пример запроса | Что делает система | Результат для пользователя |
|---|---|---|---|
| [поиск ответа по базе] | [ ] | [ ] | [ ] |
| [уточняющий вопрос] | [ ] | [ ] | [ ] |
| [многошаговый диалог / действие через инструмент] | [ ] | [ ] | [ ] |

**Исключительные сценарии.** Это половина контракта: продумайте, где можем ошибиться и как это обработано.

| Ситуация | Поведение системы | Что видит пользователь |
|---|---|---|
| ответа нет в базе | [ ] | [ ] |
| не поняли запрос | [ ] | [ ] |
| запрос вне домена / попытка вывести за контракт | [ ] | [ ] |
| инструмент не ответил / вернул ошибку | [ ] | [ ] |
| модель вернула невалидный формат или зациклилась | [ ] | [ ] |
| нужен человек: передача оператору | [ ] | [ ] |

### 2.3 Диалоговая логика и тон

- **Single-turn или multi-turn.** [ ]
- **История диалога.** Храним ли, где, сколько сообщений или токенов попадает в промпт, когда обрезаем: [ ]. *Где живёт память чат-бота? Ответ должен быть про хранилище и оркестратор, а не про модель.*
- **Долгая память о пользователе.** Есть ли профиль между сессиями, что в нём, согласие пользователя: [ ].
- **Тон и стиль.** Язык, обращение, длина ответов, эмодзи, чего не делаем: [ ].
- **Прозрачность.** Пользователь знает, что говорит с ботом: [как сообщаем].

---

## 3. Три бюджета и SLO

Бюджеты фиксируются до выбора архитектуры и становятся ограничением. Дальше архитектура подбирается под них, а не наоборот. Если кажется, что ограничений нет, поищите получше.

### 3.1 Качество

Качество ≠ «модель умнее». Качество = система выполняет контракт на нашем распределении запросов.

| Метрика (из контракта) | Как считаем | Целевое значение | Ниже чего — откат |
|---|---|---|---|
| [доля ответов со ссылкой на источник] | [judge-модель на выборке из eval-сета] | [≥ 95 %] | [< 90 %] |
| [доля корректных отказов вне домена] | [ ] | [ ] | [ ] |
| [валидность формата] | [ ] | [ ] | [ ] |

### 3.2 Задержка

Разложить типичный запрос по этапам. Числа с занятия 2: сеть и шлюз — десятки мс; retrieval — 50–300 мс; prefill промпта в 2–3k токенов — под 100 мс; decode — 5–30 мс на токен в зависимости от модели и железа; вызовы инструментов — сколько угодно.

| Этап | Оценка, мс | Откуда число |
|---|---|---|
| сеть + шлюз | [ ] | [ ] |
| retrieval | [ ] | [ ] |
| prefill (TTFT) | [ ] | [длина промпта × …] |
| decode (N токенов × TPOT) | [ ] | [N = …, TPOT = …] |
| инструменты | [ ] | [ ] |
| × число шагов агента | [ ] | [ ] |
| **Итого e2e, P50 / P95** | [ ] / [ ] | |

Целевые значения: TTFT P95 ≤ [ ] с, e2e P95 ≤ [ ] с. Стриминг: [да / нет, и почему]. *Как быстро нужно отвечать, чтобы пользователь не ушёл?*

### 3.3 Стоимость

```
cost_per_request ≈ tokens_in × price_in + tokens_out × price_out  (+ retrieval + инструменты)
cost_per_month   = cost_per_request × requests_per_month
```

| Параметр | Значение | Откуда |
|---|---|---|
| tokens_in на запрос (системный промпт + контекст + история) | [ ] | [ ] |
| tokens_out на запрос | [ ] | [ ] |
| вызовов модели на запрос | [ ] | [1 или число шагов агента] |
| price_in / price_out | [ ] | [провайдер, дата — цены живут месяцы] |
| requests_per_month | [ ] | [из 1.2] |
| **cost_per_request** | [ ] | |
| **cost_per_month** | [ ] | |

Скрытая стоимость: [люди на разметку eval, время на обвязку, GPU-часы при self-host].

Если self-host: расчёт по занятию 2 — память под веса, KV-cache на токен, число одновременных запросов на карте, ток/с при batch = 1, стоимость 1M выходных токенов при реалистичной загрузке против цены у провайдера.

### 3.4 Компромисс

Один абзац: какой бюджет главный, чем готовы жертвовать. «Стоимость доминирует: 10 млн запросов в месяц, поэтому маленькая модель с роутингом, а качество на хвосте сложных запросов принимаем ниже».

[абзац]

### 3.5 SLI и SLO

| SLI | Как считаем | SLO | Error budget |
|---|---|---|---|
| доступность | [ ] | [ ] | [ ] |
| TTFT P95 / e2e P95 | трейсы | [ ] / [ ] | [ ] |
| доля валидных по формату ответов | парсинг | [ ] | [ ] |
| groundedness / ссылка на источник | judge на выборке | [ ] | [ ] |
| доля корректных отказов | eval-сет вне домена | [ ] | [ ] |
| успешность вызова инструментов | валидные аргументы, успешный ответ | [ ] | [ ] |
| оценка на canary eval-сете | прогон на каждый релиз | не ниже baseline − [ ] п.п. | |
| пользовательский фидбек | доля 👎, эскалаций к оператору | ≤ [ ] % | |
| продуктовые метрики (по желанию) | доля возвратов, оценка полезности, NPS | [ ] | |

**Сбор фидбека.** Есть ли 👍/👎 или оценка в интерфейсе, куда попадает, кто смотрит: [ ].

Кто смотрит на дашборд и что делает, когда SLO нарушен: [ ].

---

## 4. Архитектура решения

### 4.1 Схема по семи блокам

Схема слева направо: клиент → шлюз → оркестратор → движок инференса → хранилища / инструменты → обратно; наблюдаемость под всем. Вставьте картинку или ASCII-схему.

```
[клиент] → [шлюз] → [оркестратор] → [инференс]
                         ↕
                  [хранилища] [инструменты]
        ────────── наблюдаемость ──────────
```

Для каждого блока: что делает у нас, что берём готовым, что пишем сами и почему. Правило курса: руками пишется только то, чего нет в готовом виде, или что специфично для наших данных.

| Блок | Что делает в нашей системе | Берём готовое | Пишем сами и почему |
|---|---|---|---|
| Клиент | [ ] | [ ] | [ ] |
| Шлюз | [аутентификация, лимиты, роутинг, ретраи, учёт токенов] | [LiteLLM / OpenRouter / шлюз провайдера / …] | [ ] |
| Оркестратор | [сборка промпта, вызов инструментов, агентный цикл, guardrails] | [LangGraph / PydanticAI / Agents SDK / …] | [ ] |
| Движок инференса | [API провайдера или self-host] | [провайдер / vLLM / SGLang / llama.cpp] | [ ] |
| Хранилища | [документы, индекс, память диалога, кэш, состояние агента] | [Postgres + pgvector / Qdrant / Redis / …] | [ ] |
| Инструменты | [поиск, код, внешние API, БД] | [MCP-серверы, коннекторы] | [ ] |
| Наблюдаемость | [логи, трейсы, метрики, eval в проде, фидбек] | [Langfuse / Phoenix / LangSmith + Prometheus / Loki / Sentry] | [ ] |

### 4.2 Путь одного запроса

Пошагово: какой блок что делает, куда стучимся, что кладётся в промпт на каждом вызове модели, что происходит с ответом до показа пользователю (пост-обработка, валидация формата, фильтры).

1. [ ]
2. [ ]

*Спросят со всех: где лежит база, когда мы в неё ходим, где лежит промпт, в каком порядке обрабатывается запрос.*

### 4.3 Данные и база знаний

**Чувствительность.** Содержат ли данные персональные или коммерческие сведения? Можно ли отправлять их внешнему провайдеру, или только локальная модель, или нужно маскировать перед отправкой: [ ]. Это решение определяет блок инференса.

**Источники.**

| Источник | Формат | Объём | Владелец | Как обновляется |
|---|---|---|---|---|
| [внутренняя wiki] | [HTML / PDF / Markdown / БД] | [страниц, документов] | [ ] | [вручную / парсинг по расписанию / webhook] |

**Предобработка.** Парсинг (что делаем с таблицами, картинками, сканами), очистка, дедупликация, нарезка на фрагменты (размер, перекрытие, по структуре или по длине), метаданные (дата, автор, версия, раздел): [ ]. *Что берём готовым: парсеры, чанкеры — не пишите свой, если не доказали, что готовый не подходит.*

**Индексация** (если есть retrieval). Модель эмбеддингов и почему (язык, домен): [ ]; тип поиска (векторный / BM25 / гибрид / реранкер): [ ]; параметры индекса: [ ].

**Обновление.** Как часто, инкрементально или пересборка, как удаляются устаревшие версии, кто отвечает: [ ].

**Качество знаний.**

| Метрика | Как считаем | Цель |
|---|---|---|
| покрытие: доля вопросов из eval-сета, на которые в базе есть ответ | [ ] | [ ] |
| актуальность: сколько времени от изменения источника до попадания в базу | [ ] | [ ] |
| консистентность: дубли, противоречия между документами | [ ] | [ ] |

### 4.4 Модель и промпты

- **Модель.** Какая и почему именно она: [качество на eval-сете, цена, задержка, ограничения по данным]. Если это результат сравнения — ссылка на ADR.
- **Промпты.** Из чего собирается промпт: инструкции, контекст из retrieval, история, результаты инструментов, формат ответа. Где лежат шаблоны в репозитории: [ ]. Версионируются ли и как: [ ].
- **Ограничения.** Длина контекста, лимит выходных токенов, temperature и почему такая: [ ].
- **Версионирование и откат.** Как зафиксирована версия модели (пиннинг), что делаем, если провайдер обновил модель или промпт сломал качество; как откатываемся: [ ].
- **Дообучение** (если планируется): данные, процесс, метрика, по которой поймём, что стало лучше; чем это лучше промпта и retrieval: [ ]. По умолчанию — не планируется.

### 4.5 Интеграции и интерфейсы

| С чем интегрируемся | Зачем | Протокол / способ | Кто владелец |
|---|---|---|---|
| [чат-интерфейс / мессенджер] | [ ] | [REST / WebSocket / SSE / webhook] | [ ] |
| [CRM / база пользователей / тикеты] | [ ] | [ ] | [ ] |

**Интерфейс пользователя.** Как выглядит диалог, есть ли кнопки-подсказки, стриминг, показ источников, кнопка «позвать человека»: [ ]. Мокапы — в приложениях.

### 4.6 Инфраструктура и развёртывание

- **Стек.** [контейнеры, оркестрация, serverless, просто процесс на сервере].
- **Среды.** dev / test / prod, чем отличаются, где гоняется eval: [ ].
- **Вычисления.** CPU или GPU, сколько памяти, что где живёт (индекс, история, кэш): [ ].
- **Масштабирование и отказоустойчивость.** Что делаем при росте нагрузки в 10 раз, что при падении провайдера (fallback на другую модель?): [ ].
- **Доступ.** Аутентификация, авторизация, кто видит логи: [ ].

Решения с альтернативами → ADR: [ADR-001 API vs self-host], [ADR-002 RAG vs длинный контекст], [ADR-003 агентный цикл vs фиксированный пайплайн], [ADR-004 фреймворк оркестрации], [ … ].

---

## 5. Eval-план

Без eval-сета нельзя отличить улучшение от регрессии. Он появляется до первой строчки кода.

### 5.1 Eval-сет

| | |
|---|---|
| Размер | [50–200 примеров; 20 — для КТ 1] |
| Откуда примеры | [реальные запросы / придуманные по сценариям из 2.2 / синтетика с проверкой человеком] |
| Где лежит | [ссылка на файл; формат: запрос, ожидаемый ответ или критерий, метки] |
| Покрытие | [основные сценарии, исключительные сценарии из 2.2, вне домена, попытки обхода] |
| Кто и как разметил | [ ] |
| Как пополняется | [из прода: ошибки, 👎, эскалации → в eval-сет] |

Доли по типам: [например, 60 % штатных вопросов, 20 % без ответа в базе, 10 % вне домена, 10 % многошаговых].

### 5.2 Метрики

Каждая метрика ссылается на пункт контракта из раздела 2.

| Метрика | Пункт контракта | Как считаем (точное правило / judge / человек) | Проверка judge на людях |
|---|---|---|---|
| [ ] | [ ] | [ ] | [согласие с разметчиком на N примерах: …] |

Типичные классы ошибок, которые ловим отдельно: выдумка (галлюцинация), ответ не по документам, офтопик, невалидный формат, неверный вызов инструмента, нежелательный контент.

### 5.3 Процедура

- Когда гоняем: [на каждый PR / перед релизом / по расписанию в проде].
- Canary-набор на релиз: [сколько примеров, порог отката].
- Тесты на распределении, а не на одном примере: [сколько прогонов при temperature > 0, как агрегируем].
- Что считаем регрессией и что делаем: [ ].
- Ручной ревью в проде: [выборка N ответов в неделю, кто смотрит].

### 5.4 Контроль качества в рантайме

Что стоит между моделью и пользователем: валидация формата с повтором запроса, проверка groundedness, порог уверенности с дефолтным ответом, фильтры контента, fallback на статический ответ или оператора: [ ]. Каждый механизм — это ещё задержка и стоимость; учесть в разделе 3.

---

## 6. Baseline и план усложнений

### 6.1 Baseline

Самое простое решение: один промпт, самая сильная доступная модель, без RAG и агентов. Измерить.

| | |
|---|---|
| Модель | [ ] |
| Промпт | [ссылка] |
| Результат на eval-сете | [метрика: значение, …] |
| Задержка P50 / P95 | [ ] |
| Стоимость на запрос | [ ] |
| Вывод | [достаточно / не достаточно — по какой метрике и на сколько] |

### 6.2 Усложнения

Каждое усложнение оправдывается числом на eval-сете и попаданием в бюджет. Строка без чисел «до / после» — не усложнение, а гипотеза.

| # | Что добавили | Гипотеза: какую метрику поднимет | До → после | Задержка до → после | Стоимость до → после | Решение | ADR |
|---|---|---|---|---|---|---|---|
| 1 | [RAG по документам] | [groundedness] | [ ] → [ ] | [ ] → [ ] | [ ] → [ ] | [оставили / откатили] | [ ] |
| 2 | [ ] | [ ] | | | | | |

Что решили **не** делать и почему: [ ].

---

## 7. Риски, ограничения, безопасность

### 7.1 Структурные ограничения модели и чем закрываем

Не лечится промптом — лечится обвязкой. Отметьте, какие касаются вашего продукта, и что стоит вокруг.

| Ограничение | Касается нас? | Чем закрыто (блок системы) |
|---|---|---|
| арифметика и подсчёты | [ ] | [калькулятор / код] |
| факты вне контекста | [ ] | [retrieval] |
| не знает, чего не знает | [ ] | [отказы в контракте, проверка groundedness] |
| консистентность между запросами | [ ] | [детерминированный код вокруг] |
| длинные планы без проверок | [ ] | [внешний контроль состояния, лимит шагов] |
| нет состояния между вызовами | [ ] | [хранилище + оркестратор] |
| недетерминизм | [ ] | [тесты на распределении] |
| уверенная выдумка (API, факты, код) | [ ] | [проверка человеком / тесты / structured outputs] |

### 7.2 Риски проекта

| Риск | Вероятность | Последствия | Что делаем |
|---|---|---|---|
| [провайдер обновил модель — просело качество] | [ ] | [ ] | [canary eval на релиз, пиннинг версии] |
| [данные хуже, чем казалось: PDF, дубли, устаревшие версии] | [ ] | [ ] | [ ] |
| [база знаний не покрывает реальные вопросы] | [ ] | [ ] | [ ] |
| [надёжность цепочки из n шагов ≈ pⁿ] | [ ] | [ ] | [ ] |
| [стоимость на масштабе] | [ ] | [ ] | [ ] |
| [приватность / утечка данных к провайдеру] | [ ] | [ ] | [ ] |
| [не хватит времени / людей / бюджета] | [ ] | [ ] | [ ] |

Непроверенные допущения из 1.3, которые могут всё сломать: [ ].

### 7.3 Безопасность, соответствие, этика

- **Персональные данные.** Что попадает в промпт и в логи, маскирование, срок хранения, согласие пользователя, применимые нормы (152-ФЗ, GDPR, внутренние политики): [ ].
- **Prompt injection.** Через документы, результаты инструментов, сообщения пользователя. Что делаем: [ ].
- **Нежелательный контент.** Фильтры на входе и выходе, что считаем недопустимым: [ ].
- **Действия с последствиями.** Если агент что-то делает (пишет в БД, отправляет письма, платит): подтверждение человеком, лимиты, откат: [ ].
- **Прозрачность.** Пользователь уведомлён, что это бот и что данные логируются: [ ].
- **Логи.** Кто имеет доступ, что в них хранится, как долго: [ ].

---

## 8. План работ и эксплуатация

### 8.1 Этапы

Привязать к контрольным точкам курса. Каждый этап заканчивается измеримым результатом, а не «сделали».

| Этап | Что готово | Как проверим | Срок | Ответственный |
|---|---|---|---|---|
| 0. Документирование | контракт, бюджеты, 20 запросов | КТ 1 | [ ] | [ ] |
| 1. Baseline | один промпт, число на eval-сете | 6.1 заполнен | [ ] | [ ] |
| 2. Первое усложнение | [RAG / инструменты / …] | «до → после» в 6.2 | [ ] | [ ] |
| 3. Прототип целиком | путь запроса из 4.2 работает end-to-end | демо + eval | защита | [ ] |

### 8.2 Поддержка и эксплуатация

Для учебного проекта — коротко, но честно.

- Кто чинит, если сломалось: [ ].
- Мониторинг и алерты: какие SLI из 3.5 на дашборде, при чём просыпается человек: [ ].
- Обновление базы знаний: процесс, частота, ревью: [ ].
- Обновление модели и промптов: процесс релиза, canary eval, откат: [ ].
- Метрики эксплуатации: доступность, задержка, стоимость на сессию: [ ].

---

## 9. Ресурсы

Для учебного проекта — оценка, для рабочего — обязательная часть.

- **Люди.** Кто нужен и на сколько: [ML-инженер, бэкенд, DevOps, контент / владелец знаний, разметчики для eval].
- **Вычисления и сервисы.** [GPU или API, векторная БД, трейсинг, лицензии].
- **Затраты.** Разовые (настройка, разметка, дообучение) и ежемесячные (из 3.3 плюс инфраструктура): [ ].
- **Отдача.** Сопоставить с критериями успеха из 1.1: [ожидаемая экономия, ускорение, рост удовлетворённости]. Если отдача меньше затрат — это тоже результат design doc.

---

## 10. Открытые вопросы

Что ещё не решено, кто и к какому сроку решает. Вопрос без владельца и срока переезжает в риски.

| Вопрос | Владелец | Срок | Статус |
|---|---|---|---|
| [ ] | [ ] | [ ] | открыт |

---

## 11. Приложения

- **Словарь терминов и аббревиатур** проекта: [ ].
- **Ссылки**: требования безопасности, регламенты, API-спецификации, UX-гайд: [ ].
- **Диаграммы и схемы**: архитектура, путь запроса, схема данных: [ ].
- **Мокапы интерфейса**: [ ].
- **Eval-сет и промпты**: ссылки на файлы в репозитории: [ ].
- **ADR**: список с номерами и статусами: [ ].

---

## История изменений

| Дата | Версия | Что изменилось | Почему |
|---|---|---|---|
| [ ] | 0.1 | контракт, бюджеты, 20 запросов | КТ 1 |

---

## Чек-лист перед сдачей

- [ ] В 1.1 есть критерий успеха, который не является метрикой модели.
- [ ] В 1.2 есть нагрузка с числами и цена ошибки.
- [ ] В контракте каждое обещание можно измерить, и на него есть метрика в 5.2.
- [ ] Исключительные сценарии из 2.2 расписаны: что видит пользователь, когда ответа нет.
- [ ] В каждом бюджете есть числа и написано, откуда они.
- [ ] Стоимость посчитана на месяц, а не на запрос.
- [ ] Решено, можно ли отправлять данные внешнему провайдеру (4.3), и это согласуется с выбором инференса.
- [ ] Ответ на вопрос «где живёт память» написан в 2.3 и 4.2.
- [ ] Промпты лежат в репозитории и версионируются.
- [ ] Eval-сет лежит в репозитории, а не «есть в голове».
- [ ] Baseline измерен до первого усложнения.
- [ ] У каждого усложнения есть «до → после».
- [ ] На каждое решение с альтернативами есть ADR.
- [ ] Раздел 7.1 заполнен: понятно, чем закрыто каждое ограничение, которое нас касается.
- [ ] В 7.3 есть ответ про персональные данные и prompt injection.
